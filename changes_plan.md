typing page :
- have a textbox somewhere in the page (maybe at the top?) that lets the user enter a username 

mongodb instance :
- runs locally , will have the usernames section now additionally apart from the existing things ( like the timestamps , word speeds and all )
- should have something to like get the data from this instance for the local leaderboard 

leaderboard page : 
- should have a handler to get usernames 
- display leaderboard locally instead of the global db 

